# Component Inventory

Generated by AI-DLC Reverse Engineering — 2026-08-05.

## Mobile — `src/` on `feature/crm-backed-mobile-app`

### Data layer

| File | Lines | Backend | Fate |
| --- | --- | --- | --- |
| `lib/cockpit.ts` | 496 | Cockpit CMS | **Replace** — 31 functions to re-point |
| `lib/api.ts` | ~250 | bim-crm (dead) | **Foundation** — correct conventions, no server |
| `lib/auth.ts` | 215 | Entra + Cockpit | **Keep PKCE, replace user resolution** |
| `lib/storage.ts` | 83 | local | Keep |
| `config.ts` | 44 | — | Consolidate: drop Cockpit vars, promote CRM base URL |

### Hooks and context

| File | Lines | Fate |
| --- | --- | --- |
| `hooks/useAuth.ts` | 225 | Rework — Sanctum token lifecycle |
| `hooks/useVisits.ts` | 163 | Re-point to react-query over CRM |
| `context/ThemeContext.tsx` | 172 | Keep unchanged |

### Screens — 27 components, ~9,900 lines

| Component | Lines | Domain | Migration |
| --- | --- | --- | --- |
| `CreateVisitModal` | 1109 | Visits | Re-point |
| `ClientWorkspaceScreen` | 841 | Clients | Re-point |
| `TeamReportScreen` | 784 | Reports | Re-map to CRM's 5 reports |
| `VisitDetailModal` | 650 | Visits | Re-point |
| `AdminScreen` | 606 | Admin | Re-point to CRM RBAC |
| `ui.tsx` | 497 | Design system | **Keep — the reason for this branch** |
| `ReportsScreen` | 490 | Reports | Re-map |
| `AdminQuickActions` | 446 | Admin | ✗ **DEAD — delete** (no importer) |
| `ProfileScreen` | 412 | Profile | Re-point |
| `EditVisitModal` | 408 | Visits | Re-point |
| `VisitListScreen` | 373 | Visits | Re-point |
| `VisitPlanModal` | 332 | Visits | Reconcile with `CreateVisitModal` — see duplication note |
| `AmVisitListScreen` | 286 | Visits | Re-point |
| `ClientListScreen` | 252 | Clients | Re-point |
| `CalendarBoard` | 245 | Visits | Re-point |
| `TodayDashboard` | 227 | Dashboard | Re-point to `/api/v1/dashboard` |
| `FieldControls` | 220 | Forms | Keep |
| `TeamOverviewScreen` | 158 | Reports | ✗ **DEAD — delete** (superseded by `TeamReportScreen`) |
| `PendingApprovalScreen` | 144 | Auth | Keep — **port this UX to the CRM** |
| `LoginScreen` | 135 | Auth | Rework for `/api/auth/entra` |
| `BottomNavigation` | 75 | Nav | Extend for new domains |
| `VisitPlanSummary` | 70 | Visits | Reconcile |
| `AppHeader` | 68 | Chrome | Keep |
| `ReviewScreen` | 60 | Outcomes | Re-point |
| `WorkspaceHeader` | 35 | Chrome | Keep |
| `Banner` | 25 | Chrome | Keep |
| `MetricCard` | 20 | Dashboard | Keep |

**Duplication — RESOLVED.** `cms-integration` carries *both* generations of visit UI. The audit in
`dead-code-audit.md` settled it: `App.tsx` is 355 lines and imports **14** of the 27 components. The
older `main`-era set (`VisitPlanModal`, `VisitPlanSummary`, `ReviewScreen`, `CalendarBoard`,
`WorkspaceHeader`, `MetricCard`, `AppHeader`) plus `AdminQuickActions`, `TeamOverviewScreen`,
`FieldControls`, and `hooks/useVisits.ts` are **unreachable** — 11 files, ~1,817 lines. Deleted in
Unit E.0 per Q9=C→A. `Banner.tsx` stays (live via `ReportsScreen` and `CreateVisitModal`).

⚠️ The "Migration" column above predates that audit and marks some dead files as "re-point". Treat
`dead-code-audit.md` as authoritative where they disagree.

### Utilities and scripts

`utils/visitplan.ts`, `utils/meetingGroups.ts` (67), `utils/schedule.ts` (67), `styles.ts`, `types.ts` (126).

`scripts/` holds 7 Node scripts — `seed-cockpit.mjs`, `migrate-cockpit-v24.mjs`, `audit-cockpit.mjs`,
`patch-client-sectors.mjs`, `seed-financial-calendar.mjs`, `seed-sample-visits.mjs`,
`patch-rn-ios-toolchain.mjs`. All but the last are Cockpit-specific and become obsolete — **except as
the extraction half of a data migration** (Q6). `patch-rn-ios-toolchain.mjs` runs on `postinstall`
and must stay.

## bim-crm — reusable server components

27 controllers. The API layer wraps these rather than replacing them:

**Pipeline** — `ClientController`, `ClientGroupController`, `ContactController`, `DealController`,
`DealAttachmentController`, `DealCommentController`, `DealVisitPlanController`, `VisitPlanController`,
`SourceRegistrationController`, `SourceRegistrationAttachmentController`

**Documents** — `QuotationController`, `PublicQuotationController`, `SaleOrderController`,
`PurchaseOrderController`

**Reference** — `SectorController`, `SectorStageController`, `PipelineStageController`,
`ProductController`, `SupplierController`, `FinancialQuarterController`

**Reporting** — `DashboardController`, `ReportController`

**Admin** — `UserController`, `RoleController`, `PermissionController`, `ProfileController`,
`Auth/MicrosoftController`

**Services** (business logic worth reusing verbatim) — `QuotationNumberService` (row-locked
`QT-YYYYMMDD-NN`), `SaleOrderService`, `PurchaseOrderService`.

**Policies** — `UserPolicy`, `VisitPlanPolicy` (sector-based, aligned in `791fa57`), plus per-model
policies. Models carry `visibleTo` / `forUser` scopes for row-level visibility.
